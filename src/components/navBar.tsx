function NavBar() {
  return (
    <nav className="absolute top-0 mt-10">
      <button>Home</button>
      <button>About</button>
      <button>Projects</button>
      <button>Contact</button>
    </nav>
  )
}

export default NavBar;